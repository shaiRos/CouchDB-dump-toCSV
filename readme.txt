use couchdb-dump.sh to create dump json file for a database in a couchdb instance (see inside for instructions)

use couchdb-dump-ToCSV.js to convert that dump data to csv (change file names inside)

use git bash cmd!! not wsl/normal cmd bash to run .sh file